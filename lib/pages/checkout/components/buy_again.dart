import 'package:flutter/material.dart';

class BuyAgainCheckOut extends StatelessWidget {
  const BuyAgainCheckOut({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          Text("Again")
        ],
      ),
    );
  }
}